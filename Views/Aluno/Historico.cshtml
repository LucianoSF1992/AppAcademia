@model List<AppAcademia.ViewModels.HistoricoSemanalViewModel>

@{
    ViewData["Title"] = "Histórico Semanal";
}

<h2 class="mb-3">📅 Histórico Semanal</h2>

<p class="text-muted">
    Semana:
    <strong>
        @ViewBag.InicioSemana.ToString("dd/MM/yyyy")
        a
        @ViewBag.FimSemana.ToString("dd/MM/yyyy")
    </strong>
</p>

@if (!Model.Any())
{
    <div class="alert alert-info mt-4">
        Nenhum exercício concluído nesta semana.
    </div>
}
else
{
    <div class="list-group mt-4">
        @foreach (var item in Model)
        {
            <div class="list-group-item d-flex justify-content-between align-items-center">
                <div>
                    <strong>
                        📅 @item.Data.ToString("dddd, dd/MM")
                    </strong>
                </div>

                <span class="badge bg-success">
                    💪 @item.TotalExercicios exercícios
                </span>
            </div>
        }
    </div>
}

<a asp-action="Index" class="btn btn-secondary mt-4">
    ⬅️ Voltar
</a>