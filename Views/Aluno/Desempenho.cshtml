@model AppAcademia.ViewModels.DesempenhoSemanalViewModel

<h2>📊 Meu Desempenho Semanal</h2>

<canvas id="graficoSemanal" height="120"></canvas>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <script>
        const ctx = document.getElementById('graficoSemanal');

        new Chart(ctx, {
            type: 'bar',
            data: {
                labels: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.Dias)),
                datasets: [{
                    label: 'Exercícios concluídos',
                    data: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.Quantidades)),
                    borderWidth: 1
                }]
            },
            options: {
                scales: {
                    y: {
                        beginAtZero: true,
                        ticks: {
                            precision: 0
                        }
                    }
                }
            }
        });
    </script>
}