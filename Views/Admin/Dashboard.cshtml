@{
    ViewData["Title"] = "Dashboard Administrativo";
}

<div class="container mt-4">

    <h2>🏠 Dashboard do Administrador</h2>
    <p class="text-muted">Visão geral do sistema</p>

    <div class="row">

        <!-- INSTRUTORES -->
        <div class="col-md-3">
            <div class="card text-center mb-3">
                <div class="card-body">
                    <h6>👨‍🏫 Instrutores</h6>
                    <h3>@ViewBag.TotalInstrutores</h3>
                </div>
            </div>
        </div>

        <!-- ALUNOS -->
        <div class="col-md-3">
            <div class="card text-center mb-3">
                <div class="card-body">
                    <h6>👤 Alunos</h6>
                    <h3>@ViewBag.TotalAlunos</h3>
                </div>
            </div>
        </div>

        <!-- TREINOS -->
        <div class="col-md-3">
            <div class="card text-center mb-3">
                <div class="card-body">
                    <h6>🏋️ Treinos Ativos</h6>
                    <h3>@ViewBag.TotalTreinos</h3>
                </div>
            </div>
        </div>

        <!-- EXERCÍCIOS -->
        <div class="col-md-3">
            <div class="card text-center mb-3">
                <div class="card-body">
                    <h6>📋 Exercícios</h6>
                    <h3>@ViewBag.TotalExercicios</h3>
                </div>
            </div>
        </div>

    </div>

    <hr />

    <!-- AÇÕES RÁPIDAS -->
    <h5>⚙️ Ações rápidas</h5>

    <div class="mt-3">
        <a asp-controller="InstrutorAdmin" asp-action="Index" class="btn btn-primary me-2">
            👨‍🏫 Gerenciar Instrutores
        </a>
    </div>

</div>